import {StyleSheet} from 'react-native'
import container from './container'
import camera from './camera'
import messageWrapper from './camera-permissions-dialog'

export default styles = StyleSheet.create({
    container,
    camera,
    messageWrapper
})